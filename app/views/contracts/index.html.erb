<p id="notice"><%= notice %></p>

<h1>Contracts</h1>

<table>
  <thead>
    <tr>
      <th>Magazine</th>
      <th>Enologist</th>
      <th>Position</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @contracts.each do |contract| %>
      <tr>
        <td><%= contract.magazine.name %></td>
        <td><%= contract.enologist.name %></td>
        <td><%= contract.position.name %></td>
        <td><%= link_to 'Show', contract %></td>
        <td><%= link_to 'Edit', edit_contract_path(contract) %></td>
        <td><%= link_to 'Destroy', contract, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Contract', new_contract_path %>
<%= link_to 'Strains', strains_path %>
<%= link_to 'Enologists', enologists_path %>
<%= link_to 'Magazines', magazines_path %>
<%= link_to 'Ratings', ratings_path %>
<%= link_to 'Positons', positions_path %>
<%= link_to 'Wines', wines_path %>